public class Result {
    /**
     * 传入的是一个字符串 类似“1+1” “10+12”
     * 判断符号，然后调用对应方法，返回答案
     * 提示：
     * 获取数字可以用：split("\\D")
     * 获取符号可以用: split("\\d+")
     *
     * @return
     */
    public static int result() {
        return 0;
    }
}
